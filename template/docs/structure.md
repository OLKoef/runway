# Project Structure — <Project>

> Hierarchical code map. **Owned by Mission Control** (docs mode). Reflects the current filesystem
> state; updated after each phase reaches complete.

## <project>/

```
<project>/
├── CLAUDE.md           # project memory bus (Mission Control)
├── brainstorm/
│   ├── SESSION.md      # the spec
│   └── input/          # ingested reference material
├── phases/
│   ├── PLAN.md         # build plan
│   ├── DASHBOARD.md    # live status board
│   └── blueprints/     # per-phase blueprints
└── docs/
    ├── structure.md    # this file
    └── CHANGELOG.md    # one entry per completed phase
```

### Notes
- <add notes as the project grows>
